// Simple test to verify all tooling exports compile
use taskflow_rs::{
    generate_dot_graph, generate_html_report, DebugLogger, ExecutionProfile, LogEntry, LogLevel,
    PerformanceMetrics, Profiler, TaskStats,
};

fn main() {
    println!("All imports successful!");
}
